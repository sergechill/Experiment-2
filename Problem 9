#include <stdio.h>

int main()
{
	int i, j, x, y;
	
	printf("How many rows: ");
	scanf("%d",&x);
	printf("How many columns: ");
	scanf("%d",&y);
	
	for(i = 0; i < x; i++)
	{
		for(j = 0; j < y; j++)
			printf("*");
		printf("\n");
	}
	
	return 0;
}
